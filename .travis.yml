language: node_js
node_js:
  - "8.10.0"
sudo: required
services:
  - docker
cache:
  directories:
  - node_modules
addons:
  sshknownhosts:
  - andrewmarklloyd.com
branches:
  only:
    - master
before_script:
  - openssl aes-256-cbc -K $encrypted_0a6446eb3ae3_key -iv $encrypted_0a6446eb3ae3_iv -in deploy.enc -out deploy -d
  - chmod 600 deploy && mv deploy ~/.ssh/idrsa
 #- cd front-end
 #- npm install
 #- cd ../

 
script:
  - ssh deploy@andrewmarklloyd.com 'ls -l'
  #- ./build-ci.sh
